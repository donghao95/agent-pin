# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
target/

# TypeScript incremental
*.tsbuildinfo

# Test coverage
coverage/
.nyc_output/

# Tauri / desktop
*.msi
*.dmg
*.AppImage
*.deb
*.rpm
.tauri/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.vscode/
.idea/

# Local Agent Pin runtime data
.agent-pin/

# Local planning / proposal artifacts (not part of source)
agent-pin-proposal.html
docs/feature/

# Local debug artifacts
/-w
/dev-log*.txt
/test-minimal.json

# Rust lock files: 提交所有 Cargo.lock（含 lib crate），保证 CI 与本地依赖版本一致。
# shared 是内部契约 crate 不发版到 crates.io，提交 lockfile 利大于弊。
