# Security Policy

## 报告漏洞

如果你发现安全漏洞，请**不要**在公开 Issue 中报告。

通过 GitHub Private Security Advisory 报告：

1. 进入仓库主页
2. 选择 **Security** 标签
3. 点击 **Report a vulnerability**

请附上：

- 问题简述
- 复现步骤（最小可复现样本最佳）
- 影响范围
- 你的联系方式（可选）

## 响应时间

- 首次响应：3 个工作日内
- 修复时间：根据严重程度评估，通常 14 天内发布修复版本

## 安全设计边界

Agent Pin 的安全模型如下，贡献者修改时不要破坏：

- HTTP 服务**只监听 `127.0.0.1:4317`**，不开放局域网，不绑定 `0.0.0.0`。
- 无身份验证、无 token、无远程访问能力——这是 MVP 的明确边界，不是缺失。
- Pin 内容来源限于本机 Agent（CLI / HTTP），不接受远程来源。
- `POST /api/pins` 有 1MB 请求体大小限制，防止内存耗尽。
- image block 的 `path` 必须是绝对路径，由 CLI 负责将相对路径转绝对路径再 POST。
- 应用重启时若端口被占用，会弹窗提示并退出，不会抢占端口。

## 不在安全范围内的设计

以下行为是设计内允许的，不视为漏洞：

- 任何能在本机访问 `127.0.0.1:4317` 的进程都能创建 Pin。Agent Pin 是本地工具，不隔离本机进程。
- 关闭 Pin 窗口只代表隐藏，记录保留在 `~/.agent-pin/` 下，可恢复。删除需走管理界面。
- 持久化文件未加密，路径为 `~/.agent-pin/pins/` 与 `~/.agent-pin/state.json`。
