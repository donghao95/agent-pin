[package]
name = "agent-pin-shared"
version = "0.1.0"
edition = "2021"
description = "Shared types and validation for Agent Pin (PinDocument contract)"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
